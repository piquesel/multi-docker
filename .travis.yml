sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t piquesel/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true piquesel/react-test npm test

after_success:
  - docker build -t piquesel/multi-client ./client
  - docker build -t piquesel/multi-nginx ./nginx
  - docker build -t piquesel/multi-server ./server
  - docker build -t piquesel/multi-worker ./worker
# Log into Docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin

# Take those images and push them to docker hub
  - docker push piquesel78/multi-client
  - docker push piquesel78/multi-nginx
  - docker push piquesel78/multi-server
  - docker push piquesel78/multi-worker
